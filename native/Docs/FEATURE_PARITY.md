# Native Feature Parity Map

| Electron area | Swift native module | Status |
| --- | --- | --- |
| `src/server.js`, `/state`, `/permission` | `LocalHTTPServer` | Core hook protocol implemented |
| `src/state.js`, `state-priority.js` | `StateEngine`, `ClawdState` | Core priority/session/DND implemented |
| `agents/registry.js` | `AgentRegistry` | Agent ids/capabilities/install commands mirrored, including CodeWhale and Reasonix |
| `src/prefs.js`, `src/agent-gate.js` | `Preferences`, `PreferencesStore`, `AgentGate` | Core schema/gates implemented, including installed-intent gating for startup sync |
| `src/session-alias.js` | `SessionAliasKeys` | Keying, Kiro cwd scope, TTL pruning implemented |
| `src/permission.js` | `PermissionCoordinator`, `PermissionBubbleWindowController`, `PassiveNotificationBubbleWindowController`, `BubbleStackLayout` | Native permission/passive notification bubbles, suggestions, no-decision, follow-pet stacking, auto-close, Telegram remote approval implemented |
| Desktop pet render/input windows | `PetWindowController`, `PetAssetView`, `IdleSleepRuntime`, `SoundRuntime` | Native transparent window, WebKit inline SVG / image document rendering with AppKit fallback, JavaScriptCore first-frame fallback for trusted scripted SVGs, DOM eye script generation, global cursor polling, Cloudling pointer bridge, fallback eye-follow, objectScale and normalized HTML layout placement, theme transition fades, idle animations, mouse-idle sleep/wake, hitTest hitboxes, drag/click reactions, completion / notification sounds implemented |
| Session HUD/Dashboard | `SessionHUDWindowController`, `DashboardWindowController`, `StateSnapshot`, `SessionHUDEligibility` | HUD and Dashboard implemented, including mini-mode / mini-transition visibility gates and unpinned click-reveal with Electron-style hot-zone / grace auto-hide polling |
| Settings UI | `SettingsWindowController` | Agent, bubble, theme, mini mode, Telegram, Remote SSH, and runtime controls implemented |
| Theme loader | `ThemeManifest`, `ThemeRuntime`, `ThemeWebDocumentBuilder` | Manifest validation, built-in asset resolution, sound resolution, layout/fileViewBox/mini metadata, hitbox/reaction/eye/rendering/trustedRuntime metadata, and trusted scripted SVG first-frame snapshots implemented |
| Hook/plugin sync | `IntegrationManager` | Delegates to existing scripts for compatibility |
| Remote SSH | `RemoteSSHProfile`, `RemoteSSHRuntime`, `SettingsWindowController` | Profile CRUD, tunnel lifecycle, health probe, deploy/repair hooks, Codex monitor, terminal open, and status diagnostics implemented |
| Updater | `UpdaterRuntime`, `UpdaterService`, `UpdateBubbleWindowController` | Git-mode check/apply/relaunch and native update bubble implemented |
| Telegram approval | `TelegramApprovalRuntime`, `TelegramApprovalSidecar` | Config validation, approval message, inline keyboard callback polling implemented |
| Mobile preview/PWA | `MobilePreviewRuntime`, `/mobile-preview` | Native preview HTML route implemented |
| Terminal focus | `TerminalFocusManager` | macOS PID focus path implemented |
| Mini mode | `PetWindowController`, `PetAssetView`, `StatusMenuController`, `MiniModeLayout` | State remapping, menu crabwalk / offscreen handoff entry, menu exit parabola, settings toggle, drag-to-edge snap, hover peek, edge movement, and internal display seam clipping implemented |

Remaining native tuning work: per-pixel parity for every Electron animation and repeated manual cross-theme transition review. Desktop Calico / Cloudling cursor smokes, Cloudling mini idle / working / alert smokes, Cloudling DND mini-sleep enter/exit smokes, Calico mini idle / alert / working-fallback / DND mini-sleep enter/exit smokes, Clawd mini cold-start idle / working / alert / DND mini-sleep enter/exit smokes, and Session HUD mini-mode hide / restore, unpinned default-hide, and click-reveal smokes have been verified against the native renderer. A repeatable cross-theme visual smoke now captures desktop idle / working / alert and mini idle / working for Clawd, Calico, and Cloudling. A timed transition smoke captures mid-transition and stable frames for desktop and mini idle / working / notification cycles across those themes. A state-matrix smoke covers desktop idle / thinking / working / juggling / carrying / attention / sweeping / notification / error and mini idle / thinking / working / attention / notification / error across all built-in themes. Screenshot smokes now also run automated PNG integrity checks for dimensions, color diversity, luminance range, transparency, and Cloudling scripted-state body/glow presence. Targeted Cloudling WebKit, Clawd SVG, and Calico APNG motion smokes verify trusted scripted SVGs, playback-only inline SVGs, and image-channel APNGs are not static fallback frames after the fallback grace period. Session HUD click-reveal hot-zone / grace auto-hide is covered by native unit tests.
